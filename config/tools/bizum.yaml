tools:
  - name: payments_bizum_send
    type: http
    method: POST
    url: "http://localhost:9000/mock/payments/bizum"
    mode: dangerous
    timeout: 5000
    body:
      from: "{{ .fromPhone }}"
      to: "{{ .toPhone }}"
      amount: "{{ .amount }}"
      concept: "{{ .concept }}"
